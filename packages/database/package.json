{
  "name": "@acmvp/database",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'consumed as TS source via workspace path mapping; no compiled output needed'",
    "lint": "echo 'no linter configured'",
    "test": "echo 'no tests configured'"
  },
  "dependencies": {
    "@acmvp/config": "file:../config",
    "@acmvp/types": "file:../types",
    "@supabase/supabase-js": "^2.45.0"
  }
}
